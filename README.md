From the SurajDadral's [MiniScripts](https://raw.githubusercontent.com/SurajDadral/MiniScripts/master/ShellScripts/JavaOnTermux.sh) repo.

Hint to install it:
```console
$ ln -s JavaOnTermux.sh $BIN/jont
```

So you will can to do:
```
$ jont YourFile.java
```
